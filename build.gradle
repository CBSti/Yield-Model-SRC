subprojects {

    apply plugin: 'java'
    sourceCompatibility = 1.8

    repositories {
        mavenCentral()
    }
}

apply plugin: 'idea'

task wrapper(type: Wrapper) {
    gradleVersion = '3.5'
}
